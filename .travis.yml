language: node_js
node_js:
  - 9
cache:
  directories:
    - node_modules
script:
  - npm run deploy
  - npm test
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  on:
    branch: master